<section class="s-block s-block--full-banner s-block--full-bg">
    <salla-slider 
    <div class="full-banner-entry lazy" data-bg="{{ image.url }}">
        <div class="overlay h-full w-full {{ is_opacity?'bg-black':'' }} opacity-70 absolute top-10 left-10"></div>
        <div class="flex h-full items-center justify-center p-5 xs:p-11 relative">
            <div>
              

                {% if url and image %}
                <a href="{{ url }}" class="inline-block text-white text-sm border rounded-md font-bold px-8 py-4">{{ image }}</a>
                {% endif %}
            </div>
        </div>
    </div>
    
</section>
{#  #}